<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>DialogGenerationPlan</class>
 <widget class="QDialog" name="DialogGenerationPlan">
  <property name="geometry">
   <rect>
    <x>0</x>
    <y>0</y>
    <width>860</width>
    <height>700</height>
   </rect>
  </property>
  <property name="windowTitle">
   <string>Generation plan</string>
  </property>
  <layout class="QVBoxLayout" name="verticalLayout">
   <item>
    <widget class="QLabel" name="labelHint">
     <property name="text">
      <string>Tick what to generate (several options possible). Each option offers 3 prompt slots — Animate/keep source, Reimagine (new model &amp; background), Creative — pick one, WRITE ITS PROMPT yourself on the left (load/save it from the saved-prompts row above the editor), and adjust the A/B properties below it (untick to drop, or pick a different value); the live Final prompt is on the right.</string>
     </property>
     <property name="wordWrap">
      <bool>true</bool>
     </property>
    </widget>
   </item>
   <item>
    <widget class="QSplitter" name="splitterOptions">
     <property name="orientation">
      <enum>Qt::Orientation::Vertical</enum>
     </property>
     <property name="childrenCollapsible">
      <bool>false</bool>
     </property>
    </widget>
   </item>
   <item>
    <widget class="QDialogButtonBox" name="buttonBox">
     <property name="orientation">
      <enum>Qt::Orientation::Horizontal</enum>
     </property>
     <property name="standardButtons">
      <set>QDialogButtonBox::StandardButton::Cancel|QDialogButtonBox::StandardButton::Ok</set>
     </property>
    </widget>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections>
  <connection>
   <sender>buttonBox</sender>
   <signal>accepted()</signal>
   <receiver>DialogGenerationPlan</receiver>
   <slot>accept()</slot>
  </connection>
  <connection>
   <sender>buttonBox</sender>
   <signal>rejected()</signal>
   <receiver>DialogGenerationPlan</receiver>
   <slot>reject()</slot>
  </connection>
 </connections>
</ui>
